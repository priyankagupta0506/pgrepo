machine:
 pre:
  - pip install docker-compose
 services:
  - docker
test:
 override: 
  - docker ps -a 
  - docker -v
deployment:
 prod:
  branch: master 
  commands: 
   - sudo service docker status 
   - docker-compose up 
   - docker ps –a 
