machine:
 pre:
  - pip install docker-compose
 services:
  - docker
test:
 override: 
  - docker ps -a 
  - docker -v
deployment:
 prod:
  branch: master 
  commands: 
   - sudo service docker status 
   - docker-compose build && docker images -a 
   - docker ps –a
   - aws ecr get-login
   - $(aws ecr get-login)
